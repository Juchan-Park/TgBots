# 텔레그램 봇 토큰 (BotFather에서 발급받은 토큰)
BOT_TOKEN=your_bot_token_here

# 그룹 채팅 ID (봇을 그룹에 추가한 후 확인 가능)
GROUP_CHAT_ID=your_group_chat_id_here

# === 레거시 설정 (하위 호환성) ===
# 단일 봇 매핑을 위한 기존 설정 (bot_mapping.json이 없을 때만 사용됨)
TARGET_TOPIC_ID=your_topic_id_here
SOURCE_BOT_USERNAME=existing_bot_username

# === 새로운 다중 봇 매핑 ===
# bot_mapping.json 파일을 사용하여 여러 봇을 각각 다른 토픽으로 매핑
# 설정 관리: python config_manager.py 